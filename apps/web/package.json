{
  "name": "@storm-demo/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit -p tsconfig.json && vite build",
    "deploy": "bun run build && wrangler pages deploy dist",
    "test": "vitest run"
  },
  "dependencies": {
    "@types/node": "^25.6.0",
    "zustand": "^5.0.12"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}
